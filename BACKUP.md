# سياسة النسخ الاحتياطي واستعادة البيانات

## النسخ الاحتياطي التلقائي

يوجد سكريبت `backup.ps1` يقوم بعمل نسخة احتياطية كاملة لقاعدة البيانات يومياً.

### طريقة التشغيل اليدوي

```powershell
$env:PGPASSWORD = "admin123"
.\backup.ps1
```
